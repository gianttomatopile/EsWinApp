<UserControl x:Class="EsWinApp.Views.UserControls.MultiplicationMatrix"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EsWinApp.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button Content="Increase" Click="Increase_Click" Width="100" FontSize="16" Margin="5"/>
            <Button Content="Decrease" Click="Decrease_Click" Width="100" FontSize="16" Margin="5"/>
        </StackPanel>

    </Grid>
</UserControl>
